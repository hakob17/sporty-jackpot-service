{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(mvn *)",
      "Bash(docker compose *)",
      "Bash(./scripts/smoke-test.sh*)",
      "Bash(scripts/smoke-test.sh*)",
      "Bash(curl http://localhost:8080/*)",
      "Bash(jq *)"
    ],
    "deny": [
      "Bash(rm -rf *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r \".tool_input.file_path // .tool_response.filePath // empty\" | grep -q \"src/main/java\" && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Main source changed - the Docker image builds with -DskipTests, so `mvn test` is the only gate. Run it (plus a smoke test for behaviour changes) before reporting this done.\"},\"suppressOutput\":true}' || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
